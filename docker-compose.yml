version: '3'
services: 
  web-app:
    build: 
      context: .
      dockerfile: Dockerfile.dev
    ports: 
      - "4000:4000"
    volumes: 
      - /usr/app/web-app/node_modules
      - .:/usr/app/web-app
